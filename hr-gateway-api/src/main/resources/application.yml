server:
  port: 8080
spring:
  application:
    name: hr-gateway-api
  cloud:
    gateway:
      routes:
      - id: hr-worker
        uri: lb://HR-WORKER
        predicates:
        - Path=/hr-worker/**
      - id: hr-payroll
        uri: lb://HR-PAYROLL
        predicates:
        - Path=/hr-payroll/**
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
          